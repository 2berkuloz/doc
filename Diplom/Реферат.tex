\abstract{РЕФЕРАТ}

Объем работы равен \formbytotal{lastpage}{страниц}{е}{ам}{ам}. Работа содержит \formbytotal{figurecnt}{иллюстраци}{ю}{и}{й}, \formbytotal{tablecnt}{таблиц}{у}{ы}{}, \arabic{bibcount} библиографических источников и \formbytotal{числоПлакатов}{лист}{}{а}{ов} графического материала. Количество приложений – 2. Графический материал представлен в приложении А. Фрагменты исходного кода представлены в приложении Б.

Перечень ключевых слов: интерфейс, клиентская сторона, разработка, элементы интерфейса, кнопки, меню, поля ввода, иконки, визуальная часть, стиль, графический интерфейс, текстовый интерфейс, текст, вайрфрейм, функциональность. референс, контент, текст-рыба, триггер, уведомления, мокап, логотип, компоненты, виджеты, ui-кит, набор графических элементов, фирменный стиль, шапка, хеддер, тело сайта, боди, чекбокс, пикер, алерт окно, тост, кебаб меню, бургер кнопка, диалог, сообщения, логин, пароль, пользователь, логика взаимодействия, анализ требований интерфейса, разметка, роли, прототип, интерактивный прототип, бумажный вайрфрейм, юзабилити, пользовательский опыт, пользовательский интерфейс, доступность, переходы, анимации, тестирование, баг, дефект, скринбар, бегунок скролла, скролл, отладка, консоль, адаптивный дизайн, медиазапросы, карта сайта, хеширование, апи, функциональное тестирование, нефункциональное, брейкпоинты, сессия, токен, кукис, авторизация, регистрация, аутентификация, индикатор, валидация.

Объектом разработки является клиентская сторона закрытого корпоративного мессенджера. Элементы, которые видит пользователь и с которыми взаимодействует при использовании веб-приложения. Такими элементами являются: кнопки, меню, диалоговые окна, поля ввода, иконки, графические элементы и навигация по чатам. Объект разработки ввключает в себя визуальную состовляющую (дизайн) и функциональную (механизмы взаимодействия), направленные на создание удобного, интуитивно понятного и визуально привлекательного опытя для пользователей мессенджера, который позволяет им легко общаться и взаимодействовать с другими людьми. 

Целью выпускной квалификационной работы заключается в создании эффективной, удобной, простой и интуитивно понятной среды для внутренней коммуникации и совместной работы сотрудников, которая обеспечит и поспособствует повышению продуктивности и достижению бизнес-целей компании.

В процессе разработки пользовательского интерфейса закрытого корпоративного мессенджера выделяются основные сущности, которые напрямую влияют на дизайн (UI и UX) и механизмы взаимодействия с пользователем. Именно эти сущности формируют ядро информационной архитектуры и определяют, как пользователи будут воспринимать и использовать веб-приложение. Ключевыми сущностями, определяющими дизайен интерфейса, являются: пользователь, сообщение, чат, группа, вложения, файлы, уведомления.

При разработке системы использовалась собственная архитектура с использованием клиент-серверной модели взаимодействия. Клиентская сторона, реализованная на HTML, CSS и JavaScript, отвечает за формирование пользовательского интерфейса и обработку взаимодействия на стороне пользователя.

\selectlanguage{english}
\abstract{ABSTRACT}
  
The volume of work is \formbytotal{lastpage}{page}{}{s}{s}. The work contains \formbytotal{figurecnt}{illustration}{}{s}{s}, \formbytotal{tablecnt}{table}{}{s}{s}, \arabic{bibcount} bibliographic sources and \formbytotal{числоПлакатов}{sheet}{}{s}{s} of graphic material. The number of applications is 2. The graphic material is presented in annex A. The layout of the site, including the connection of components, is presented in annex B.

List of Keywords: interface, client-side, development, interface elements, buttons, menu, input fields, icons, visual part, style, graphical user interface, text interface, text, wireframe, functionality, reference, content, placeholder text, trigger, notifications, mockup, logo, components, widgets, UI-kit, set of graphic elements, corporate identity, header, body (of website), checkbox, picker, alert window, toast, kebab menu, burger button, dialog, messages, login, password, user, interaction logic, interface requirements analysis, layout, roles, prototype, interactive prototype, paper wireframe, usability, user experience, user interface, accessibility, transitions, animations, testing, bug, defect, scrollbar, scroll slider, scroll, debugging, console, adaptive design, media queries, sitemap, hashing, API, functional testing, non-functional testing, breakpoints, session, cookies, authorization, registration, authentication, indicator, validation.

The object of development is the client-side of a closed corporate messenger. These are the elements that the user sees and interacts with when using the web application. Such elements include: buttons, menus, dialog boxes, input fields, icons, graphic elements, and chat navigation. The object of development encompasses both the visual component (design) and the functional component (interaction mechanisms), aimed at creating a convenient, intuitive, and visually appealing experience for messenger users, allowing them to easily communicate and interact with other people.

The goal of the final qualifying work is to create an effective, convenient, simple, and intuitive environment for internal communication and employee collaboration, which will ensure and contribute to increasing productivity and achieving the company's business goals.

In the process of developing the user interface for the closed corporate messenger, key entities are identified that directly influence the design (UI and UX) and user interaction mechanisms. These entities form the core of the information architecture and determine how users will perceive and use the web application. The key entities defining the interface design are: user, message, chat, group, attachments, files, notifications.

The system was developed using a custom architecture based on a client-server interaction model. The client-side, implemented using HTML, CSS, and JavaScript, is responsible for forming the user interface and processing user-side interactions.
\selectlanguage{russian}
